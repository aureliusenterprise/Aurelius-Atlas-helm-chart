---
# Example values for service

# container image
# https://github.com/keycloak/keycloak-containers/blob/16.1.1/server/README.md
keycloak:
  tag: "16.1.0"
  env:
    KEYCLOAK_USER: "admin"
  realm_file_name: "realm_m4i.json"
  persistence:
    enabled: true
    # storageClass: "-"
    accessMode: ReadWriteOnce
    size: 1Gi
  external_ip: 35.195.200.158
  external_hostname: "google.aureliusenterprise.nl"
  keycloakFrontendURL: "https://google.aureliusenterprise.nl/"

atlas:
  tag: "2.2.0.4"
  persistence:
    enabled: true
    # storageClass: "-"
    accessMode: ReadWriteOnce
    size: 1Gi
  external_hostname: "google.aureliusenterprise.nl"

reverse-proxy:
  tag: "1.0.9.3"
  external_hostname: "google.aureliusenterprise.nl"

host_ip: 35.195.200.158
external_hostname: "google.aureliusenterprise.nl"

elastic:
  elastic_search:
    replicaCount: 1
    version: 8.2.2
    persistence:
      enabled: true
      # storageClass: "-"
      accessMode: ReadWriteOnce
      size: 4Gi
  kibana:
    replicaCount: 1
    version: 8.2.2
  enterprise_search:
    replicaCount: 1
    version: 8.2.2
  external_hostname: "google.aureliusenterprise.nl"

flink:
  repository: wombach/docker-flink
  tag: 1.15.1.0
  taskmanager_numberOfTaskSlots: 10
  external_hostname: "google.aureliusenterprise.nl"

ingress:
  email_address: gabriella.pascha@aureliusenterprise.com
  dns_url: "google.aureliusenterprise.nl"

python-rest:
  external_hostname: "google.aureliusenterprise.nl"
