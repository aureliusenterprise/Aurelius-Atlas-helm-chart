---
replicaCount: 1
repository: wombach/docker-apache-atlas
pullPolicy: Always
tag: "2.2.0.4"
service:
  name: atlas
  type: ClusterIP
  externalPort: 21000
  internalPort: 21000
  internalPort_kafka: 9027
persistence:
  enabled: true
  # storageClass: "-"
  accessMode: ReadWriteOnce
  size: 1Gi
env:
  KEYCLOAK_ATLAS_ADMIN_USERNAME: "atlas"
  # ATLAS_EXTERNAL_URL: "http://atlas.{{ .Release.Namespace }}.svc.cluster.local:21000/api/atlas"
  # KEYCLOAK_SERVER_URL: "https://{{ .Values.global.external_hostname }}/{{ .Release.Namespace }}/auth/"
#external_hostname: "aureliusdev.westeurope.cloudapp.azure.com"
